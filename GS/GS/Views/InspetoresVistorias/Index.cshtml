@model IEnumerable<InspetoresVistorias>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Inspetores e suas vistorias</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Criar Novo</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Inspetor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vistoria)
        </th>
        <th>
        </th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Inspetor.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vistoria.Id)
        </td>

        <td>
            <a asp-controller="InspetoresVistorias" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Edit</a> |
            <a asp-controller="InspetoresVistorias" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Details</a> |
            <a asp-controller="InspetoresVistorias" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
        </td>
    </tr>
}
    </tbody>
</table>
